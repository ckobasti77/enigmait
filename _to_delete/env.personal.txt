# =============================================================
# ENIGMA COMMAND CENTER — .env.personal
# Lične referentne vrednosti + mesta za tajne. NIJE za deploy.
# VAŽNO: odmah dodaj ".env.personal" u .gitignore!
# Tajne (tokeni, ključevi) čuvaj i u password manageru.
# =============================================================

# --- Google nalog (sve Google stvari žive na ovom nalogu) ---
GOOGLE_ACCOUNT=enigmaitrs@gmail.com

# --- Google Cloud projekat (GA4 service account + Google Ads OAuth client idu OVDE) ---
GOOGLE_CLOUD_PROJECT_NAME=enigmait
GOOGLE_CLOUD_PROJECT_ID=enigma-command-center
GOOGLE_CLOUD_PROJECT_NUMBER=251155175686

# --- Google Ads ---
# Manager (MCC) nalog — developer token je vezan za njega
GOOGLE_ADS_MANAGER_CUSTOMER_ID=107-719-1135
# Advertiser nalog (Enigma IT, EUR, Beograd TZ) — linkovan pod MCC 15.8.2026
GOOGLE_ADS_CUSTOMER_ID=629-228-3044
# Token: MCC -> Admin -> API Center -> "View token". Basic Access prijava poslata 15.8.2026.
GOOGLE_ADS_DEVELOPER_TOKEN=
GOOGLE_ADS_API_CONTACT_EMAIL=office@enigmait.rs
# Popunjavaš kod P17 (OAuth client u Cloud projektu "enigmait"):
GOOGLE_ADS_OAUTH_CLIENT_ID=
GOOGLE_ADS_OAUTH_CLIENT_SECRET=
GOOGLE_ADS_OAUTH_REFRESH_TOKEN=

# --- GA4 (popunjavaš pre P4) ---
GA4_PROPERTY_ID=
# service account JSON se lepi u Settings ekran aplikacije, ne ovde;
# putanju do fajla drži samo lokalno ako hoćeš:
GA4_SERVICE_ACCOUNT_JSON_PATH=

# --- Convex app (pre P2/P3, unosi se u Convex dashboard -> Environment Variables) ---
# generiši: openssl rand -hex 32
CREDENTIALS_ENCRYPTION_KEY=
RESEND_API_KEY=
EMAIL_FROM=

# --- Meta app (pre P7; isti app služi i OpenReply) ---
INSTAGRAM_APP_ID=
INSTAGRAM_APP_SECRET=
FACEBOOK_APP_SECRET=
META_GRAPH_API_VERSION=v25.0

# --- Meta Pixel / Conversions API (pre P11, ide u enigma-digital Vercel env) ---
META_PIXEL_ID=
META_CAPI_ACCESS_TOKEN=
LEAD_VALUE_EUR=

# --- Meta Ads (pre P12; System User token iz Business Managera) ---
META_ADS_SYSTEM_USER_TOKEN=
META_AD_ACCOUNT_ID=

# --- OpenReply (pre P6; read-only korisnik cc_reader na Railway Postgresu) ---
OPENREPLY_DB_URL=

# --- Ads write ograde (pre P15) ---
ADS_WRITE_ENABLED=false
BUDGET_MIN_EUR=
BUDGET_MAX_EUR=
